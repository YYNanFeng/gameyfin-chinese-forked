# Gameyfin 汉化说明

本文档说明已实施的中文本地化更改。

## 已完成的更改

### 前端（React + TypeScript）

1. **国际化基础设施**
   - 添加了 `i18next` 和 `react-i18next` 依赖
   - 创建了 `app/src/main/frontend/i18n.ts` 配置文件
   - 默认语言设置为简体中文（zh-CN）
   - 支持从 localStorage 读取用户语言偏好

2. **翻译文件**
   - `app/src/main/frontend/locales/en.json` - 英文翻译
   - `app/src/main/frontend/locales/zh-CN.json` - 简体中文翻译
   - 包含导航、路由、登录、通用词汇等核心 UI 字符串

3. **已本地化的组件和视图**
   - `routes.tsx` - 所有路由标题已本地化
   - `MainLayout.tsx` - 导航栏、页脚等已本地化
   - `LoginView.tsx` - 登录页面已本地化
   - `App.tsx` - 更新以支持动态页面标题

4. **语言切换器**
   - 创建了 `LanguageSwitcher.tsx` 组件
   - 在导航栏中显示地球图标按钮
   - 用户可以在中文和英文之间切换
   - 选择的语言会保存到 localStorage

### 后端（Kotlin + Spring Boot）

1. **邮件模板已本地化**
   - `email-confirmation.mjml` - 邮箱确认
   - `password-reset-request.mjml` - 密码重置请求
   - `user-invitation.mjml` - 用户邀请
   - `account-enabled.mjml` - 账户启用

2. **邮件标题已本地化**
   - `MessageService.kt` 中所有邮件标题已改为中文：
     - 密码重置请求
     - 等待审批
     - 账户启用/禁用/删除
     - 账户警告
     - 邮箱确认
     - 用户邀请
     - 测试通知

## 如何构建和运行

### 开发模式

```bash
# 在项目根目录
./gradlew bootRun
```

### 生产构建

```bash
# 构建整个项目（包括前端和后端）
./gradlew build
```

## 如何添加更多翻译

1. **前端翻译**
   - 编辑 `app/src/main/frontend/locales/zh-CN.json` 添加新的翻译键
   - 在组件中使用 `useTranslation` hook：
     ```typescript
     import { useTranslation } from 'react-i18next';
     
     function MyComponent() {
       const { t } = useTranslation();
       return <div>{t('your.translation.key')}</div>;
     }
     ```

2. **后端邮件模板**
   - 直接编辑 `app/src/main/resources/templates/messages/*.mjml` 文件
   - 修改 `MessageService.kt` 中的邮件标题文本

## 语言切换

用户可以通过导航栏右上角的地球图标切换语言：
- 🌐 图标 → 选择 "简体中文" 或 "English"
- 选择会自动保存并在下次访问时生效

## 未来改进建议

1. 使用 Vaadin 的 I18N provider 实现更系统的国际化
2. 添加更多语言支持（繁体中文、日语、韩语等）
3. 本地化更多视图和组件（管理页面、设置页面等）
4. 本地化日期、时间格式
5. 本地化数字格式
6. 添加语言自动检测（基于浏览器设置）
